# DESIGN — What（灵魂：功能点清单）

> 一句话定位：**把 CONTEXT 里的每个痛点，翻译成一个可实现、可验收的功能点。**
> 功能点是 AI agent 的最小执行单元。其它内容（架构/API/数据模型）都是功能点的实现细节。
> 更新时机：加功能、改模块边界、API 变化、数据结构变化。

---

## 功能点清单（主线）

> 每个功能点必须能回答：解决了 CONTEXT 里哪个痛点？

| # | 功能 | 触发方式 | 解决的痛点 | 说明 |
|---|------|---------|-----------|------|
| 1 | **[功能名]** | [快捷键/按钮/自动] | 痛点 N | [一句话描述做什么] |
| 2 | **[功能名]** | [...] | 痛点 N | [...] |

---

## 系统架构（辅助）

```
[ASCII 图描述整体结构]

例：用户 → [UI层] → HTTP → [服务层] → [外部服务/数据]
```

**[一句话说清楚各层职责划分]**

---

## 模块清单（辅助）

### [层名，如：Backend]

| 文件/模块 | 职责 | 关键约束（不能做什么） |
|----------|------|---------------------|
| `[文件名]` | [做什么] | [约束] |

### [层名，如：Frontend]

| 文件/目录 | 职责 |
|----------|------|
| `[路径]` | [做什么] |

---

## API 契约（辅助）

> 这是前后端的对接点，精度要求 90 分以上，写错要返工。

```
POST /[path]
  body:    { "field": type }
  returns: { "field": type }
  注意：[重要的业务规则]

GET /[path]
  returns: { "field": type }
```

---

## 数据模型（辅助）

```json
{
  "field": "type — 说明"
}
```

---

## AI 调用设计（辅助，如有）

| 功能 | endpoint | 模型 | 备注 |
|------|----------|------|------|
| [功能] | /[path] | [model] | [说明] |

Prompt 分层：
- `CONTEXT/DESIGN/PLAN` → 给 Claude Code（agent）读，构建项目
- `prompts.py` → 给业务 AI 模型读，驱动功能
- `eval/scenarios.yaml` → 给 infra-model-eval 读，验证效果

---

## 工程规范（辅助）

- [规范1，如：所有 prompt 只在 prompts.py 定义，其他文件禁止内联]
- [规范2，如：API 只绑 127.0.0.1，不对外暴露]
- [规范3，如：数据文件路径从 config 读，不硬编码]
